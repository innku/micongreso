<div id="rest">
  <h2 class="section">Inicio de sesión</h2>
  <div class="left">
    <div id="interna" class="propuesta session">
			<%= form_for @user_session, :url => user_session_path, :html => {:id => "login_form"} do |f| %>
			  <% if @user_session.errors.any? %>
				  <div id="errorExplanation">
				    <h2>Error al intentar accesar</h2>
						<ul>
							<% @user_session.errors.full_messages.each do |msg| %>
								<li><%= msg %></li>
							<% end %>
						</ul>
				  </div>
				<% end %>

				<p>
					<%= f.label :email %><br/>
					<%= f.text_field :email, :class => "input" %>
				</p>
				<p>
					<%= f.label :password %><br/>
					<%= f.password_field :password, :class => "input" %>
				</p>
				<p>
					<%= f.check_box :remember_me %> <%= f.label :remember_me %>
				</p>

			  <%= f.submit "Iniciar Sessión", :class => "action" %>
				<p class="forgot clear"><%= link_to "Se me olvidó mi contraseña", new_password_reset_path %></p>
			<% end %>
    </div>

  </div>
  <div class="right"></div>
</div>


